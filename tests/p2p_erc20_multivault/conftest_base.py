# Re-export everything from securitize conftest_base since MultiVault uses the same Loan struct
from tests.p2p_erc20_securitize.conftest_base import (  # noqa: F401
    BPS,
    ZERO_ADDRESS,
    ZERO_BYTES32,
    AggregatorV3LatestRoundData,
    EventWrapper,
    FullLiquidationRedeemedResult,
    FullLiquidationResult,
    LoanExtensionOffer,
    Offer,
    RedeemResult,
    SecuritizeLoan,
    Signature,
    SignedLoanExtensionOffer,
    SignedOffer,
    SignedRedeemResult,
    SignedWalletValidation,
    WalletValidation,
    calc_collateral_from_ltv,
    calc_full_liquidation,
    calc_full_liquidation_redeemed,
    calc_ltv,
    calc_partial_liquidation,
    compute_liquidity_key,
    compute_loan_id,
    compute_securitize_loan_hash,
    compute_signed_offer_id,
    get_calls,
    get_events,
    get_last_event,
    get_securitize_loan_mutations,
    manipulate_signature,
    replace_namedtuple_field,
    sign_extension_offer,
    sign_kyc,
    sign_offer,
    sign_redeem_result,
)
