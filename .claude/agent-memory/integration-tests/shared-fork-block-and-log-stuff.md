---
name: shared-fork-block-and-log-stuff
description: One shared Ethereum-mainnet fork block (25400000) + one generated log_stuff for all integration suites; which suites are on which RPC; the pre-existing multivault non-despxa V2/Midas gap
metadata:
  type: project
---

Outcome of the feat/despxa-loop review cleanup (2026-07-13). Read before touching integration fork config or the `log_stuff` event workaround.

**ONE shared Ethereum-mainnet fork block.** `tests/conftest_base.py` defines `ETH_FORK_BLOCK = 25400000`
(NOTE: an older version of this note said `tests/fork_helpers.py` — the constants + `build_erc20_contract_def_with_log_stuff` actually live in `tests/conftest_base.py`; there is no `fork_helpers.py`) (chosen >= the deJAAA CentrifugeOracleAdapter deploy ~25022228 so despxa/Centrifuge flows work without a divergent block). ALL suites forking `BOA_FORK_RPC_URL` import and use it — never hardcode a block literal in a conftest again:
- `tests/p2p_erc20_multivault/integration/conftest.py` (was 24541820; the separate `despxa_env`@25400000 was DELETED and despxa now reuses the dir's ordinary fixtures)
- `tests/p2p_erc20_securitize/integration/conftest.py` (was 25300898)
- `tests/p2p_erc20_vaulted/integration/conftest.py` (was 25300898)
- `tests/registrar_connector/integration/conftest.py` (was 25300898)
NOT unified (different chain, `BOA_FUJI_FORK_RPC_URL` @ 47222291, Avalanche Fuji): `tests/p2p_erc20_vaulted/profitr/conftest.py`. Always grep the conftest's `os.environ[...]` before assuming a suite is Ethereum mainnet — the review's list named profitr but it's Fuji.

BASE (2026-07-14): the multivault integration dir ALSO now has a `BASE_FORK_BLOCK = 48500000` in `tests/conftest_base.py` for the deSPXA/Base suite (`test_loop_despxa.py`), which forks a base-mainnet RPC derived from `BOA_FORK_RPC_URL` (`eth-mainnet`->`base-mainnet`). One block per chain, same rule. It has its OWN `base_boa_env` + Base-scoped facet/kyc/market/oracle/token fixtures (a Base fork is a different env than the Ethereum `boa_env`). See `[[despxa-base-fork]]`. Adding BASE_FORK_BLOCK did NOT touch the other ETH_FORK_BLOCK consumers (securitize/vaulted/registrar collect cleanly).

**ONE generated `log_stuff`.** `tests/fork_helpers.py::build_erc20_contract_def_with_log_stuff(main_source_path, main_name, base_def, [facet_defs])` GENERATES the dummy `log_stuff()` from compiler data (`used_events` of the facets ∪ `event_defs` of base, `base.`-prefixing base-declared events). NEVER hand-maintain event field lists — a stale hand-written list silently broke the whole multivault dir when `LeveragedLoanCreated.mint_deadline` was added. Now used by BOTH the multivault unit conftest AND the multivault/securitize/registrar integration conftests. Each caller needs a `*_base_contract_def` fixture (`P2PLendingMultiVaultBase` / `P2PLendingSecuritizeBase` / `P2PLendingVaultedBase`) + the facet defs. Facets: multivault = Loan+Liquidation+Refinance; securitize/vaulted = Refinance+Liquidation (no separate Loan facet).

**despxa fixtures collapsed.** With one block, the ~23 duplicated `despxa_*` fixtures are gone. `test_loop_despxa.py` now uses the dir's common `borrower`/`lender`/`usdc`/`kyc_validator_contract`/`owner`/`accounts`. Only Centrifuge-specific handles remain as fixtures: `despxa_dejaaa`, `despxa_oracle`, `despxa_async_vault`, `despxa_manager`, `despxa_hook`, `despxa_asset_id`, `despxa_vault_impl`, `despxa_keeper`, `p2p_usdc_dejaaa`. The fulfil/whitelist helpers are now plain MODULE FUNCTIONS in the integration conftest (imported by the test): `despxa_whitelist(hook, addr)`, `despxa_fulfill_deposit(manager, asset_id, controller, assets, shares)`, `despxa_fulfill_redeem(manager, asset_id, controller, shares, assets)` — NOT fixtures returning closures. Style rule the user wants: fixtures set up real objects; behaviour is plain functions taking explicit args.

**Oracle price ranges at block 25400000** (the vaulted `test_create_*::test_oracle_data` sanity checks, which hard-pin a ~$1-wide range and say "Must change if fork block changes"): acred 110077745000 (~$1100.78, 8dec); decrdx 997241988974137000 (~$0.99724, 18dec); dejaaa 1037386254473350000 (~$1.03739, 18dec); dejtrsy 1028903789003160000 (~$1.02890, 18dec); mfone 110038212 (~$1.1004, 8dec); scope 125447165600 (~$1254.47, 8dec). The `test_create_loan` bodies size collateral off the live oracle (`oracle.decimals()`-relative) so they self-adjust — only the human-readable range asserts needed editing. xPRISM stays `@pytest.mark.skip`: its cUSDO/USD feed `0x5b79480B...`'s `aggregator` is still `0x0` (unset on-chain) at 25400000, so no price — permanent, not block-fixable.

**Whale/registry/Midas state at 25400000:** USDC whale `0x99C9fc46...` still solvent; Securitize registry + ACRED oracle + the V2 VaultRegistrar `0xD280bcA6...` all fine (securitize integration 13/13 green). The hardcoded multivault borrower `0x81aF...1009` is ALREADY a registered Securitize investor at this block (re-registering reverts "Wallet already exists").

**RESOLVED (2026-07-14): the non-despxa suite below is now GREEN on this branch** — the V2-connector + Midas-admin fixes described here were applied (see `[[multivault-nondespxa-v2-midas]]`), and a full run of `tests/p2p_erc20_multivault/integration` is 24/24 (5 deJAAA + 5 deSPXA + 14 non-despxa). The historical context below is kept for reference.

**PRE-EXISTING (was red at the time, now fixed): multivault NON-despxa integration suite.** At HEAD the dir's conftest referenced `contracts/SecuritizeRegistrarV1Connector.vy` which was DELETED — so create/settle/replace/transfer/add_collateral/remove_collateral/loop_acred/loop_mfone were ALL crashing at fixture load before any of my changes (despxa was the only green group via its own env). I migrated the conftest to the V2 connector (`SecuritizeRegistrarV2Connector` + `VaultRegistrarV2_abi` @ `0xD280bcA6...`, funded `securitize_owner`/`token_issuer`), which advances them past the crash to two GENUINE remaining gaps that need a separate effort:
  1. ACRED tests: V2 `registerVault` needs a per-investor EIP-712 `set_investor_signature` (see `SecuritizeRegistrarV2Connector.register_vault` -> empty deadline/sig reverts `0x5335c859`). The securitize suite does this because its borrower is a KEY-CONTROLLED fresh account (`sign_register_vault` helper). The multivault borrower is a hardcoded address with no key -> the test BODIES must be rewritten to use a key-controlled borrower + thread `set_investor_signature`.
  2. mfone tests: `MidasAccessControl.grantRole` from `MIDAS_DEFAULT_ADMIN` fails at 25400000 (admin role rotated since 24541820) -> the greenlist autouse needs the CURRENT Midas admin re-derived on-chain.
Do NOT weaken assertions to make these pass; they need the real rewrites above. They were never green on this branch, so leaving them red is not a regression.
